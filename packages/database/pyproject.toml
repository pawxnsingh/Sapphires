[project]
name = "database"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aditya Gupta", email = "realadityagupta2@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "prisma>=0.15.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
database = "database:main"

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
